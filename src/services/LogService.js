/**
 * Init third party log clound service.
 *
 * @function
 */
function init() {
  // Sentry can be used
}

/**
 * Error logg.
 *
 * @param {string} error
 */
function log(error) {
  console.error(error);
}

export default {
  init,
  log
};
